namespace protocol;

enum error_code : int
{
    OK = 0,
    FAILED = 1,
}

enum id : int
{
    PING                                    = 0,
    PONG                                    = 1,
    ESTABLISH                               = 2,

    LOGIN_REQUEST							= 2001,
	LOGIN_RESPONSE							= 2002,
}

